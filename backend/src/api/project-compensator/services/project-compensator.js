'use strict';

/**
 * project-compensator service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::project-compensator.project-compensator');
