'use strict';

/**
 * project-compensator router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::project-compensator.project-compensator');
